[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "alaetheia"
version = "0.0.0"
description = "Alaetheia learning laboratory skeleton"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
